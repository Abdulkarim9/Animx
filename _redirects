# Netlify redirects
# Redirect all routes to index.html for client-side routing
/* /index.html 200

# Specific redirects for JSON files to ensure they're found
/animations/:category/index.json /animations/:category/index.json 200 